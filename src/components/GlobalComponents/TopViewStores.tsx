import React from 'react'
import { Store } from '../../types/data/storeInterface';

const TopViewStores = () => {
  const getTopViewStores = () => {
    // data.Store.map((store:Store[]) =>  )
  }
  return (
    <div>
      
    </div>
  )
}

export default TopViewStores
